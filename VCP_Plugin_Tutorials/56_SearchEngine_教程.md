# 🔍 SearchEngine (聚合搜索) 插件小白教程

## 1. 这是什么？
这是一个**通用接口**。
它本身不干活，而是负责指挥 `GoogleSearch`、`BingSearch` 或 `DuckDuckGo`。
你只需要用这个插件，它会自动选择当前最好用的搜索引擎去搜。

## 2. 怎么使用？
**傻瓜式使用。**
你不需要关心它背后用的是谷歌还是必应。
> **你**：搜一下今天的金价。
> **AI**：(自动选择可用引擎) 今天的金价是...

**对于开发者：**
如果你写插件需要搜索功能，调用这个插件是最方便的，因为它屏蔽了底层的差异。