[2026-03-13] - Rosa
[17:05] VChat前端更新分析(六)：Agent配置安全性大升级 + 分块渲染器 + AA可视化
分析范围：69d04c8 ~ 40b18ae (4 commits, 2026-03-11 ~ 03-13)

## 1. Agent大小写错位修复 (40b18ae) ⭐⭐⭐⭐⭐ 最重磅
### 问题根因
AgentId大小写不一致导致config读写错位→配置丢失。
### 三处同步修复
#### prompt-manager.js (+22行)
- switchMode()重构：先获取systemPrompt→切换mode→一次性合并写入(mode+prompt)→UI更新
- triggerAgentSave改为fire-and-forget(.catch)不再await，减少阻塞
#### prompt-sponsor.js (+78行)
- 新增normalizeId(agentId)全局统一toLowerCase
- loadAgentConfig: 3次重试(100/200/500ms指数退避)，空文件检测
- saveAgentConfig完全重写：文件锁(lockFile, flag:'wx'创建) + 5秒超时 + 过期锁清除 + 原子写入(tmp→rename) + 自动备份(.backup) + finally清理锁和临时文件
#### agentConfigManager.js (+87/-57行)
- 新增normalizeId()实例方法，所有公开方法入口统一调用
- readAgentConfig: 新增retryCount参数，统一瞬态错误重试(ENOENT/SyntaxError/CONFIG_EMPTY三种都重试3次100/200/500ms)，替代旧的仅ENOENT重试+嵌套循环
- 缓存key统一用normalizeId后的id
- 默认模型从gemini-2.5-flash-preview改为gemini-2.0-flash-exp

## 2. 分块渲染器优化 (69d04c8)
### JS: 新增Role Divider渲染
- ROLE_DIVIDER_REGEX匹配<<<[ROLE_DIVIDE_SYSTEM]>>>等标记
- transformSpecialBlocks()中渲染为<div class="vcp-role-divider role-system type-start">
- 支持3角色(System/Assistant/User) × 2动作(起始/结束)
### CSS: +434/-210行
主要是格式化重排(单行→多行)，提升可读性，实际新增样式少量

## 3. AgentAssistant V2可视化 (4f9a7ae)
RAG_Observer.html新增displayAgentAssistantNotification():
- 匹配条件: data.source === 'AgentAssistant'
- 金色主题卡片(--accent-agent)
- 状态点: Succeed绿/Failed红/其他橙
- marked.parse()渲染Markdown内容
- 配套rag-observer-config.js新增消息类型匹配
对应VCP后端AA V2的委托完成/失败广播

## 4. ToolBox数字输入修复 (7b2c80d)
VCPHumanToolBox/renderer.js: 数字input新增min/max/step属性支持
config.js: LightMemo tag_boost加min:0/max:1/step:0.01约束

## RAG Observer消息类型更新（累计7类）
RAG_RETRIEVAL_DETAILS(蓝) / META_THINKING_CHAIN(紫) / AGENT_PRIVATE_CHAT_PREVIEW(金) / AI_MEMO_RETRIEVAL(青) / DailyNote(青) / AGENT_DREAM_*(粉) / AgentAssistant(金,新增)
Tag: vchat文档, Agent配置安全, normalizeId, 大小写修复, 文件锁, 原子写入, Role Divider, AgentAssistant可视化, 分块渲染器, 2026-03-13