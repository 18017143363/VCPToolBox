[2026-05-10] - Rosa
[15:20] Dedalus代理 Claude thinking 真流式 XML标签修复。
主人发现“思维链的 XML 标签怎么又变回 [thinking] 了”。Rosa读取并分析 `E:\酒馆部署\dedalus-proxy\dedalus_proxy.py` 后确认根因：`convert_claude_thinking_brackets_to_xml()` 只在 `force_claude_nonstream_sse` 假流式合成分支中调用；恢复 `FORCE_CLAUDE_THINKING_NONSTREAM_SSE=False` 后，Claude thinking 走真实上游 stream，direct passthrough 只是 `await out.write(chunk)`，绕过 XML 转换，因此上游原始 `[Thinking: ...]` 被直接透传。
修复过程：
1. 首个补丁脚本 `patch_stream_think_xml.py` 因完整函数文本锚点过死失败，报 `convert_claude_thinking_brackets_to_xml anchor not found`。
2. 改写 v2 脚本：用 `def validate_or_repair_json_body` 前插入 helper，更稳；替换 direct passthrough 分支。
3. v2 执行成功，返回 `PATCHED_STREAM_THINK_XML_V2`，备份：`dedalus_proxy.py.bak_stream_think_xml_v2_20260510_151952`，验证项 `HasFilter=True / HasLineConverter=True / HasDirectClaudeFilter=True`。
最终改动：新增 `ClaudeThinkingStreamXMLFilter` 状态机和 `convert_chat_sse_line_thinking_to_xml()`，在 Claude thinking 真流式 direct 分支解析 OpenAI Chat Completions SSE `data:` 行，改写 `choices[].delta.content` 开头的 `[Thinking: ...]` 为 ``；非 Claude 仍原样透传。
注意：需要重启 Dedalus 代理生效。若 Dedalus 实际 SSE 字段不是 `choices[].delta.content`，需抓真实 chunk 继续适配。
Tag: Dedalus代理, ClaudeThinking, 真流式, XML标签, think标签, SSE过滤器, direct passthrough, 状态机, 代码补丁