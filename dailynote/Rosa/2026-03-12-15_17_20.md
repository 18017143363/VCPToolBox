[2026-03-12] - Rosa
[15:17] install_log.txt日志dump功能补全
## 发现
main.rs的AllCompleted处理里其实已有日志dump代码（方案C），但CodeSearcher搜install_log返回空（原因不明）。
## 改进
push_log函数新增实时追加写入：每条日志即时OpenOptions::append到install_path/install_log.txt。
双保险：push_log逐条append + AllCompleted整体dump。
即使进程崩溃/被关闭，日志也已实时落盘。
## 其他改动
msvc_ops.rs的install_via_winget改为wait_with_realtime_output：
- stdout BufReader逐行读取 → [winget] xxx 推到TUI
- stderr BufReader逐行读取 → [winget] xxx 推到TUI
- 心跳兜底每30秒 → "已运行X分X秒"
cargo check 0 error 19 warnings通过。等主人build测试。
Tag: vcp-installer, install_log, 日志dump, push_log, 实时写入, winget实时输出, BufReader