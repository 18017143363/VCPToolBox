[2026-03-28] - Rosa
[14:22] Comfy Cloud逆向项目重启讨论
主人重新提起Comfy Cloud（cloud.comfy.org）逆向计划，目标是完整能力复刻。
## 情报更新
- VSearch Grok模式搜索确认：零公开逆向资源，免费层API被服务端锁死
- 免费层400积分/月，仅限Web UI使用，API需订阅
- 核心突破口：Web UI完全可用 → 浏览器会话劫持路线
## 已有资产
- ComfyCloudHacker目录：E:\VCP\Plugin\ComfyCloudHacker\
- recon_userdata.js（Phase 1B侦察脚本）已就绪
- config.env配好connect模式（port 9222）
## 方案升级讨论
主人提出用VCPDesktop（桌面渲染系统）替代纯Puppeteer方案。
可能方向：桌面挂件做前端控制面板、嵌入webview、或结合ChromeBridge/ScreenPilot三位一体。
待主人确认具体技术路线。

[14:35] VCPDesktop源码级可行性验证 — 重大发现！
## 核心发现：挂件拥有完整原生网络能力
源码验证结果：
1. widgetManager.js的buildSandboxCode()只代理了document，**未劫持fetch/WebSocket/window**
2. vcpProxy.js的vcpAPI.fetch()本身就是原生fetch的认证包装
3. widgetFS提供saveFile/loadFile/listFiles持久化存储能力
4. 挂件内可直接使用：window.fetch、XMLHttpRequest、WebSocket、window.VCPDesktop

## 方案B进化版
不需要webview！VCPDesktop挂件本身就是全能客户端：
- 原生fetch直接调cloud.comfy.org API
- WebSocket实时监听生成进度
- widgetFS持久化存储认证token
- vcpAPI.post()内置AI能力（优化提示词）
- 收藏系统实现挂件持久化

## 唯一风险：CORS
CSP connect-src *解除客户端限制，但服务端可能不返回CORS头。
三个应对方案：1.Electron webSecurity:false 2.VCP后端代理 3.主进程IPC桥接

[17:00] 完整通信链路验证成功
## DesktopRemote双向通信确认
- QueryDesktop → 获取挂件ID和收藏状态
- ViewWidgetSource → 读取挂件完整HTML源码
- widgetFS.saveFile() → 挂件写结果到持久化目录
- FileOperator.ReadFile → Rosa读取结果文件
## 完整API逆向成果
图片下载URL确认：GET /api/view?filename={hash}.png&subfolder=&type=output → 302重定向CDN
完整调用链：auth/session → ws → prompt → jobs轮询 → view下载
## PoC v2验证
addEventListener绑定事件解决了onclick在Shadow DOM沙箱中失效的问题。
查积分、下载图片功能均正常运行。
Tag: Comfy Cloud, VCPDesktop, 通信链路, DesktopRemote, QueryDesktop, ViewWidgetSource, widgetFS, PoC验证

[15:02] CORS测试结果 — 全部通过！
## BrowserWindow配置确认
desktopHandlers.js第1755行，desktop窗口webPreferences：
- contextIsolation: true
- nodeIntegration: false
- 无webSecurity配置（默认true）
## 实测结果（5项全通过）
1. 原生fetch可用 ✅ typeof=function, native code
2. httpbin公共API跨域 ✅ status=200
3. **Comfy Cloud跨域 ✅ status=200, 拿到8002字符完整HTML**
4. WebSocket ✅ echo.websocket.org连接成功
5. fetch未被沙箱代理 ✅ 原生native code
## 结论
尽管webSecurity默认true，但Electron的file://协议下发起的跨域请求实际上不受CORS限制！
VCPDesktop挂件可以直接fetch cloud.comfy.org，无需任何代理/webview/Chrome浏览器。
方案B进化版完全可行，下一步：登录认证token获取 + API端点逆向。

[16:20] HAR抓包分析完成 — API体系完全破解！
## 认证机制
- 双层认证：Bearer Firebase JWT（~1h有效）+ Cookie session（2h有效）
- POST /api/auth/session 用JWT换Session Cookie
- JWT直接暴露在WebSocket URL参数中
- Firebase项目ID: dreamboothy
## 已确认API端点（12个）
1. POST /api/auth/session — 认证
2. GET /api/users — 用户信息
3. GET /api/settings — 设置
4. GET /api/jobs?status=...&limit=N — 任务列表/状态
5. GET /api/features — 功能开关
6. GET /api/object_info — 节点定义
7. GET /api/global_subgraphs — 模板列表（25+个）
8. GET /api/global_subgraphs/{name} — 模板工作流JSON
9. GET /api/assets?include_tags=models — 模型列表
10. POST /api/internal/cloud_analytics — 分析
11. GET api.comfy.org/customers/cloud-subscription-status — 订阅状态
12. WSS /ws?clientId={uuid}&token={jwt} — 实时通信
## 账户状态
FREE tier, has_fund=true, renewal 2026-04-04
## 工作流JSON格式
与本地ComfyUI完全一致（nodes/links结构），抓到Z-Image模板完整JSON
## 缺失
第二次HAR抓包成功！POST /api/prompt完整逆向！

[16:45] 第二次HAR分析 — POST /api/prompt完整破解
## POST /api/prompt 请求结构
- 认证：Bearer JWT + Cookie session（双层）
- Body: {client_id, prompt, extra_data}
- prompt字段：标准ComfyUI节点图格式（与本地完全一致）
- extra_data.auth_token_comfy_org: JWT再传一次
- 响应：{node_errors:{}, prompt_id:"b67024a6..."}
## 任务生命周期追踪
- pending(0s) → in_progress(8.6s排队) → completed(14.4s执行)
- 总耗时~23秒
- completed响应包含: filename, display_name, mediaType, nodeId, workflow_id
## 使用的模板
Z-Image-Base文生图：z_image_bf16 + qwen_3_4b + ae.safetensors
KSampler: steps=25, cfg=4, sampler=res_multistep, scheduler=simple
## 剩余缺口
第二次HAR抓包成功！POST /api/prompt完整逆向！

[16:45] 第二次HAR分析 — POST /api/prompt完整破解
## POST /api/prompt 请求结构
- 认证：Bearer JWT + Cookie session（双层）
- Body: {client_id, prompt, extra_data}
- prompt字段：标准ComfyUI节点图格式（与本地完全一致）
- extra_data.auth_token_comfy_org: JWT再传一次
- 响应：{node_errors:{}, prompt_id:"b67024a6..."}
## 任务生命周期追踪
- pending(0s) → in_progress(8.6s排队) → completed(14.4s执行)
- 总耗时~23秒
- completed响应包含: filename, display_name, mediaType, nodeId, workflow_id
## 使用的模板
Z-Image-Base文生图：z_image_bf16 + qwen_3_4b + ae.safetensors
KSampler: steps=25, cfg=4, sampler=res_multistep, scheduler=simple
## 剩余缺口
图片下载URL格式未确认（HAR中无直接图片下载请求，可能通过WebSocket推送或/view端点）
Tag: Comfy Cloud, POST /api/prompt, 完整逆向, 任务生命周期, Z-Image, KSampler, 里程碑
Tag: Comfy Cloud, CORS测试, VCPDesktop, 全部通过, 里程碑, Electron file协议, 跨域无限制
Tag: Comfy Cloud, VCPDesktop, 源码验证, 网络能力, CORS, 方案B进化, widgetManager, vcpProxy, buildSandboxCode
Tag: Comfy Cloud, 逆向, VCPDesktop, 方案讨论, 桌面挂件, 项目重启