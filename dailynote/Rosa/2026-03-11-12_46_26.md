[2026-03-11] - Rosa
[12:43] VCP/VChat前后端GitHub更新
## VCP后端 (d23ba68 → 9b5a700)
- 159个对象, 256.35KB
- 处理了3个冲突：
  1. VCPForumOnline插件：4个untracked文件与上游冲突，删除本地接受上游新插件
  2. AgentAssistant.js：上游新增activeSessionLocks(Set)和activeDelegations(Map)两个变量用于委派/持久对话追踪，本地无此修改→保留上游
  3. rag_params.json：上游新增timeDecay配置{halfLifeDays:30, minScore:0.5}，本地无此项→保留上游
- notepad编辑器PTY问题→用git commit --no-edit -m跳过
## VChat前端 (b41d966 → 4f9a7ae)
- 11个对象, 3.75KB, Fast-forward零冲突
- 4文件变更：messageRenderer.css大改(+465/-211), RAG_Observer.html(+29), messageRenderer.js(+16), rag-observer-config.js(+3/-1)
## 深度分析完成 — 26个commit核心变更

### 1. TagMemo V6 — LIF Spike Propagation (ae5d559)
KBM核心升级：[4.5]步骤从"逻辑拉回"→"仿脑认知扩散"
- 所有Tag作为种子节点(带膜电位)，2跳迭代扩散(MAX_HOPS=2)
- 触发门槛0.10，突触衰减0.3，微电流<0.01丢弃
- 单节点扇出≤20，涌现节点硬截断≤50
- 种子取max(原权重,涌现能量)防循环膨胀
- 本质：Tag共现矩阵当图神经网络邻接矩阵做消息传递

### 2. AgentAssistant V2全面重构 (f644c4d + f66ff48 + eaa0b31 + 374509e)
- 异步任务委托: task_delegation参数→后台有界对话循环(最多15轮)→TaskComplete标记→回调通知
- 占线锁: activeSessionLocks防并发，finally保证释放
- 积分系统: 成功+5分，agent_scores.json持久化
- 排行榜: AdminPanel新页面可视化
- 放弃系统: TaskFailed标记，Agent可主动放弃无法完成的任务

### 3. RAG TimeDecay (64b7726 + 9b5a700, 作者Miaotouy)
- TimeDecay修饰符，半衰期/最低分/目标标签
- Score = Score * 0.5^(days/halfLife)
- 语法分隔符从冒号改为竖线，避免和K乘数冲突

### 4. DailyNote附加文件 (cfd4e5c)
- processLocalFiles()：本地文件路径→复制到image/file目录→生成ImageServer URL
- 支持Markdown图片和普通链接两种格式
- 注意：日记内容中不能包含完整的文件协议URL，否则会触发附件处理逻辑

### 5. RAG模块拆分 (9b5a700, 预留接口605行)
ContentSanitizer(207行) + RAGAlgorithmEngine(167行) + RAGCacheManager(133行) + TimeDecayProcessor(89行)

### 6. 其他
- VCPForumOnline v3.1.0(1266行) + 巡航插件(306行)
- ZImageTurboGen新插件 + CapturePreprocessor重构
- 向量化崩溃bug修复 + LightMemo时间范围语法 + 分布式监控器
Tag: 日记, VCP更新, TagMemo V6, LIF Spike, AgentAssistant V2, 委托系统, 积分, TimeDecay, DailyNote附件, 代码分析
Tag: 日记, VCP更新, VChat更新, git操作, 冲突处理, VCPForumOnline, AgentAssistant, timeDecay