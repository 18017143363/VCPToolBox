[2026-04-02] - Rosa
[12:50] cloud_workflow_params.json全貌分析——138种节点类型的完整参数清单

## 发现
之前错误地认为只有_mergeParams里的20个用户参数，被主人纠正后读取了cloud_workflow_params.json(631KB)。

## 实际参数规模
- **138个节点类型**（class_type），每个都有自己的COMBO下拉参数
- 不同生态用完全不同的采样节点：KSampler(标准63种sampler) vs WanVideoSampler(21种wan专用scheduler) vs CogVideoSampler vs LTXVBaseSampler
- CLIPLoader.type有19种值：stable_diffusion/sd3/lumina2/wan/flux2/qwen_image/chroma/hidream/ace/omnigen2/ovis/longcat_image...
- UNETLoader.weight_dtype: default/fp8_e4m3fn/fp8_e4m3fn_fast/fp8_e5m2
- DualCLIPLoader.type有12种值（和CLIPLoader不完全一样）
- 一些all-in-one Loader（RadianceUnifiedLoader/FluxLoader/ClownModelLoader/SD35Loader）把unet+clip+vae+lora全塞进一个节点，参数极多
- 高级采样器如ClownSampler有独特参数：noise_type_sde/noise_mode_sde/implicit_sampler_name/overshoot_mode等

## 对方案C的影响
1. 骨架配方(recipe)仍然是核心——不同生态的节点拓扑和连接完全不同
2. 查表(model_families)的参数覆盖范围远超之前预估的10个字段
3. 每个族需要指定的不仅是clip/vae/sampler/scheduler/steps/cfg，还可能包括weight_dtype/clip_type/device/precision等节点特有参数

## 关键结论
recipes.json的骨架配方 + model_families.json的查表覆盖，两层架构方向正确，但查表的字段数量比之前估计的多很多。

[13:35] cloud_node_defs.json提取成功——713KB，3400个节点类型
### 数据来源
cloud_object_info.json(9.9MB)是base64编码的/api/object_info响应，decode后7.42MB。
用extract_node_defs.js提取Loader/Sampler等关键节点的combos+defaults。

### 关键发现
1. **数值defaults可直接用**：KSampler.steps=20/cfg=8, WanVideoSampler.steps=30/cfg=6/shift=5, EmptySD3LatentImage.width=1024等
2. **COMBO的default是字母排序第一个，不可用**：UNETLoader.unet_name=acestep(字母A), CLIPLoader.clip_name=clip-vit
3. **不同采样器参数集完全不同**：WanVideoSampler多了force_offload/rope_function/batched_cfg等

### 方案C最终架构确定
三层优先级：用户传参 > model_families族指定 > cloud_node_defs节点默认值
- 族只需指定模型文件名类参数（clip/vae/clip_type/recipe）
- 数值参数自动从node_defs取（大幅减轻族定义的工作量）

[13:51] ComfyCloudGen.js v0.3.0改动完成
### 改动内容（2处ApplyDiff）
1. `new WorkflowBuilder()`多传cloud_node_defs.json和model_families.json路径
2. `parseMode()`加入"传了unet/checkpoint但没传recipe→走模式A"分支
### 完整链路
用户传unet+prompt → parseMode返回A → WorkflowBuilder.build()内部matchFamily自动匹配族 → 三层合并(用户>族>node_defs) → 构建工作流 → 提交云端
Tag: ComfyCloudGen, cloud_workflow_params, 138节点类型, 参数全貌, 方案C修正, 节点可变参数