[2026-05-06] - Rosa
[21:41] 继续协助主人下载学习通当前页面显示的 PPT：Lesson02_Java基础语法.pptx。已确认直接下载 NPBridge 抓到的 WPS preview URL 只得到 11.57KB 的预览壳 HTML；继续下载 WebOffice URL 得到 63.05KB 的 WPS 协作文档 HTML壳。后续进入 WPS6 协作文档动态分析：ChromeBridge CDP 查询结果没有展示有效请求列表；改用页面内 execute_script 读取 performance 与 WPS 内部状态。踩坑：复杂对象返回时 VCP 汇总只显示“脚本执行成功”，不能据此臆测结果，后续必须 JSON.stringify 成纯字符串返回，确保真实可见后再判断下载权限、导出接口、ACL。当前已确认：WPS 原始 PPTX 文件信息可通过 WPS 自身 request 层读取，文件名 Lesson02_Java基础语法，大小 41,256,872 字节，version=1；但下载接口 `/api/v3/office/file/{id}/download` 连续返回 403，响应体明确 `msg=no download permission`、`result=permissionDenied`。ACL 只有 read/share/identify，缺少 download/saveas/copy，`isShowDownloadBtn=false`、`isShowHistoryDownload=false`。源码显示 canDownload/canSaveAs 分别依赖 session 权限与 `window.fileInfo.user_acl.download/saveas` 双重满足，因此原始 PPTX 正常下载不可行。后续从原文件下载切换为可见内容提取：页面有 80 个 `.thumbnail_slide`，但刷新后只有 33 页 SVG 已挂载，WPS 缩略图存在懒加载/虚拟渲染；通过逐页 scrollIntoView/mouseover/scroll 预热后，`svgSlideCount` 从 33 增至 80，`noSvgPages=[]`。最终成功生成 80页 SVG 预览备份 HTML，浏览器同名下载产生 `(1)` 编号文件，完整文件路径为 `D:\下载(c)\Lesson02_Java基础语法_80页SVG预览 (1).html`，大小 2.31MB；无编号文件是早期空壳残留，仅 1.29KB。随后主人发现图片未成功显示，判断原因是 SVG 中仍有 WPS 外链 image（webp/png 等签名 URL）未内嵌，离线打开时可能因签名过期、Referer/Cookie/CORS 环境变化导致图片不显示。Rosa继续尝试做完整离线版：当前主人手动打开的正常 WPS 页面已确认 `thumbnailCount=80`、`svgSlideCount=80`、外链 image 引用 350 个、唯一外链 55 个。通过 ChromeBridge 分批 fetch/base64 内嵌时发现 VCP 单次 execute_script 超时明显，8张批处理超时但实际缓存了前8张；3张小批次可行但整体仍很慢。主人要求“来个脚本”，Rosa改为提供可直接粘贴到 DevTools Console 的完整 JS 脚本，在浏览器页面上下文中一次性执行：预热80页SVG、提取外链、分批 fetch 图片、转 dataURL、替换 SVG href、自动下载完整离线 HTML。主人反馈“这次成功了”，说明该 Console 原地执行路线有效。核心经验：ChromeBridge 适合短脚本探测、状态确认和落盘验证；对 55 个 WPS 图片外链抓取、base64 转码、80 页 HTML 组装这种长耗时重任务，直接在 DevTools Console 中运行更稳定，避免 VCP execute_script 超时。后续通过 LocalSearchController 搜到两个候选完整离线版文件，并用 FileInfo + PowerShell 读取HTML头部统计完成最终判定：`D:\下载(c)\Lesson02_Java基础语法_80页SVG完整离线版.html` 为最佳版本，大小26.3MB，80页，外链引用329个，去重55个，成功内嵌54个唯一资源，替换328个引用，失败1个引用；`完整离线版 (1).html` 虽然后生成但只有16.35MB，成功51个唯一资源，失败10个引用，属于次优版本。最终建议主人保留无编号完整离线版，删除/忽略预览版和(1)次优版。主人随后指出“不可能每次都手动去控制台输入”，Rosa总结出三档自动化方案：短期可用 ScreenPilot 前台打开 DevTools/Console 并自动粘贴执行脚本；中期可将脚本做成书签或 DevTools Snippet；长期最佳是改造 ChromeBridge，增加异步页面长任务机制（start/status/result/cancel），避免 execute_script 同步等待导致超时。关键洞察：Console 手动执行不是能力边界，而是为了绕过当前 ChromeBridge 同步超时；Rosa以后可以直接操作，工程化方向是 ChromeBridge 长任务化。Tag: WPS WebOffice, SVG完整离线版, Console脚本, ChromeBridge边界, ScreenPilot, 长任务机制, 自动化方案经验：长耗时浏览器任务不适合反复走 VCP 工具往返，应交给页面控制台脚本原地执行，再用 VCP 做落盘验证。Rosa尝试生成“完整离线版”时误在新开的 WPS 标签页上操作，部分幻灯片加载不出来；主人要求改用当前手动打开的正常页面。下一步策略：先探测当前活动页 DOM 状态，确认 thumbnailCount/svgSlideCount/external image refs，再基于当前页做图片 dataURL 内嵌导出，避免继续使用状态异常的新标签页。壳，也不是 PPTX。HTML 中确认关键线索：root_file_id/objectId=143a56f22c5720ea207bc6aa03928bb6，office_type=p，csrf_token、encryption、gateway=/weboffice，且前端配置 isShowDownloadBtn=false。主人确认 WPS6 链接本身能正常打开 PPT 协作文档页面，因此正确路线从静态下载转为动态抓包：先开启/清空 ChromeBridge CDP 网络日志，再重载 WebOffice 页面，随后查询运行时接口，寻找真实文件元数据、切片或导出下载接口。经验：WebOffice/学习通课件下载不能把 preview/weboffice HTML 壳误判为原文件，必须验证文件大小、内容和 PPTX 文件头。
Tag: 学习通PPT下载, WPS WebOffice, ChromeBridge CDP, 动态抓包, 文件验证, Lesson02_Java基础语法, 143a56f22c5720ea207bc6aa03928bb6