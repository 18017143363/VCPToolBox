[2026-03-07] - Rosa
[00:03] nexos2api容器重建
主人不小心删了nexos2api Docker容器，需要重建。
## 情况
- 容器被删除，但代码和配置文件仍在E:\nexos2api
- 重建只需docker compose up -d --build
- 上次配置：端口3001，handlerId=bac916ea(Gemini 3.1 Pro Preview)
## 问题
PowerShell PTY在newSession后恢复正常。
## 重建结果
1. BOM修复：Dockerfile首行有UTF-8 BOM（EF BB BF），用PowerShell字节操作移除
2. 镜像源：原ustc/163/daocloud三个源全挂，主人手动在Docker Desktop Engine设置中清空mirrors（走代理直连）
3. docker compose up -d --build → 1.5秒构建完成（大部分层CACHED）
4. 容器启动成功：nexos2api-nexos2api-1 Up，端口3001
5. API测试：401 Unauthorized — Cookie过期！需要重新登录nexos.ai获取新Cookie
## 待办
## 完整修复过程
1. Cookie更新：主人提供新Cookie（账号huangpeiyong802@gmail.com），写入.env（UTF-8无BOM）
2. docker compose restart不会重载.env → 必须docker compose down + up
3. Cookie认证通过后404 "Chat not found" — 因为docker compose up没加--build，容器内config.js还是旧的！
4. handlerId更新：主人通过Console脚本获取新UUID 49e76f42-6966-418c-b253-3a5a98247ea1
5. config.js中3处旧UUID(d052e384)替换为新UUID(49e76f42)
6. docker compose down + up -d --build → 1.4秒构建完成
7. API测试成功："Hello! 👋 How can I help you today?"
## 新踩坑
- docker compose up不加--build不会重新COPY代码进镜像！改了本地文件但容器内还是旧的
- docker compose restart也不会重载.env环境变量
## parent_id冲突修复（补丁v3）
### 问题
nexos.ai消息链机制要求每条新消息带parent_id指向最新消息。DISABLE_HISTORY=true时代码跳过获取lastMessageId→请求无parent_id→400冲突。
### 旧补丁(v1)失效原因
v1方案是每次创建新chat（通过chat.data的302重定向获取新chatId），但nexos.ai已将chat.data从302改为200+RSC格式（text/x-script），正则匹配不到chatId了。
### v3修复方案
index.js L194: `if (!disableHistory)` → `if (true)` — 始终获取history中最新消息ID作为parent_id。
### 验证
连续3次请求(apple/banana/cherry)全部成功，之前第2次就400。
### 额外补丁（已写入但v3后不再需要）
L130: forceNewChat逻辑（v2补丁，已被v3方案替代但保留在代码中）
## 当前配置
- 端口：3001
- handlerId：49e76f42-6966-418c-b253-3a5a98247ea1
- 账号：huangpeiyong802@gmail.com
- 预算：€5（到期2026-03-13）

Tag: 日记, nexos2api, 容器重建, 完整修复, BOM修复, 镜像源, Cookie更新, handlerId更新, parent_id补丁v3, docker build踩坑, 踩坑清单#9-#12