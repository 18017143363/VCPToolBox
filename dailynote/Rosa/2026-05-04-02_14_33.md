[2026-05-04] - Rosa
[02:13] 继续开发 NeuralPhantomBridge，完成 NP端 Connector 落地

主人给出两个源码路径：`E:\VCP\VCPChrome` 和 `E:\VCP\neural-phantom-4.7`，让我继续写插件。
本轮工作：
1. 读取 VCPChrome 的 background/content_script/manifest/popup，确认其浏览器端 WebSocket 客户端连接范式。
2. 读取 Neural Phantom 真实 background/manifest，并用 PowerShell 定位 page-script/inject 的消息入口。
3. 确认真实字段结构：Mock规则、本地文件替换、WS改写规则、断点、恢复请求、URL替换等。
4. 新增 NP端 `vcp-connector.js`，通过 WebSocket 连接 VCP端 `NeuralPhantomBridge`。
5. 修改 NP background 尾部，追加 `importScripts('vcp-connector.js')`。
6. 修复 VCP端 config.env 编码问题、registerRoutes签名问题、WebSocket server重复启动问题、TrafficStore重复请求问题。
7. 静态检查：NP端/VCP端 JS 均 `node --check` 无错误输出；未做运行时联调。

关键反思：
- 不能相信单一工具结果：FileOperator SearchFiles 曾把 VCPChrome 搜成0文件，实际目录有源码。
- ChromeBridge 的真实源码非常关键，直接推翻了我之前对 `registerRoutes` 第二参数的错误假设。
- 这次写 Connector 前逐字段核对 page-script/inject，避免了“看起来能连但命令无效”的烂坑。
Tag: 日记, NeuralPhantomBridge, NP端Connector, VCPChrome, 插件开发, 源码核对, WebSocket, Rosa开发记录