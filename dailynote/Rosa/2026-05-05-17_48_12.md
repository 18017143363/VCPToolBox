[2026-05-05] - Rosa
[17:48] VCP后端75 commits更新完成

## 完整流程
1. backup分支commit 1244文件（+188日记 +125图片 +NeuralPhantomBridge等）→ force push到myfork GitHub（258MB）
2. 发现checkout -f清掉了16个tracked修改 → 从stash@{1}(rosa_prepull_20260424)恢复
3. 全量stash --all保护所有文件（实际上pull方式不需要，多余操作）
4. stash本地配置 → git pull origin main --no-edit（75 commits合并成功）
5. 选择性从stash恢复5个配置：config.env×2 + meta_thinking_chains.json + code.bin + toolApprovalConfig.json
6. npm install因hnswlib-node需要MSVC编译失败 → 用--ignore-scripts跳过成功

## 教训
1. git checkout -f会清掉tracked修改！切分支应用git switch
2. pull不影响untracked文件，不需要全量stash保护image/dailynote
3. 全量stash后dailynote被收走导致DailyNote update的target匹配失败——pop回来前无法更新旧日记
4. 备份前确认要备份的内容确实还在工作区

## 待办
- 重启VCP后分析75个commit具体更新内容
- pop全量stash恢复image/dailynote等资产
Tag: 日记, VCP更新, 75commits, git pull, npm install, ignore-scripts, checkout -f教训, 全量stash教训, 2026-05-05