[2026-05-10] - Rosa
[16:43] Dedalus代理补丁审计完成

主人反馈真流式状态机有bug（正文被写进thinking标签），要求回顾所有补丁准备重构。

## 审计结果
从5/1到5/10共22个备份文件，代码从16KB膨胀到45KB（近3倍）。
关键膨胀点：
- ⑤⑥ /v1/messages适配尝试（+8KB死代码，从未成功）
- ⑩ upstream_body_debug（+5KB调试helper）
- ⑰⑱⑳㉑ 多版本状态机残留

## 承重墙清单
1. 干净headers修复Content-Length截断
2. Claude thinking自动注入+max_tokens补足+temperature=1
3. JSON repair兜底
4. sticky key + client_max_size=50MB + 402自动删key
5. 假流式合成SSE（保留为开关兜底）

## 死代码清单
- /v1/messages适配函数（⑤⑥）
- responses reasoning映射（⑦）
- 调试helper函数（⑩）
- 多版本状态机残留

## 重构方案
建议方案B：以production_cleanup(39KB)为基线，只追加真流式开关+一个正确的状态机，避免v3/v4/v5污染残留。等待主人确认。

[16:48] 主人选择直接回退到413_client_max_size(17KB)基线
[16:58] 在17KB干净基线上重建Claude thinking——极简方案
只在非bypass分支检测Claude模型后注入thinking参数，不做任何格式转换/假流式/状态机。
改动仅~20行：检测anthropic/claude模型→注入thinking={type:enabled, budget_tokens:1024}→max_tokens自动补足→temperature=1→重新序列化body。
上游返回的[Thinking: ...]原样透传，不转XML，不搞状态机。
ApplyDiff提交后读回验证（Select-String确认第327-337行），落盘成功。
[17:10] 真因定位：上游Dedalus在流式SSE中丢失`]`闭合符
[17:18] 进一步逆向分析 Dedalus 为什么把流式 thinking 处理成未闭合格式。
当前代理已是 17KB 干净基线，只注入 thinking/max_tokens/temperature，并在 direct stream 分支原样 iter_any→out.write，不再做状态机/假流式/XML转换。因此 `[Thinking: ...]` 是 Dedalus 上游格式。
422 真因已确认不是参数错，而是旧 Content-Length 截断；改为干净 headers 后 stream=true 返回 200，说明 `/v1/chat/completions` 接受 thinking 参数。
流式未闭合的核心推断：Dedalus 非流式路径拿完整 Anthropic message blocks 后 stringify 为 `[Thinking: {thinking}] {text}`；流式路径则把 Anthropic 的 thinking_delta 与 text_delta 都拍扁成 OpenAI `delta.content`，只在 thinking block start 时发 `[Thinking: `，但在 thinking block stop 或 text block start 时忘了发 `] `，导致正文直接粘进 thinking。
重要洞察：这是结构信息丢失，不是少一个字符那么简单。到代理侧只剩 content 字符串，无法 100% 判断 thinking/text 边界。之前状态机吞正文，正是因为上游根本不发 `]`，状态机永远等不到闭合。
建议：保留极简注入，不再在 chat/completions 拍扁流上写猜测状态机；若要真流式+可靠边界，应验证 Dedalus `/v1/messages` 或 Anthropic native stream 是否能暴露 content_block_start/delta/stop 结构，再从结构化事件转换。
Tag: Dedalus代理, ClaudeThinking, 上游Bug, 流式未闭合, 结构信息丢失, Content-Length, 真流式, 逆向分析
通过在direct passthrough加`log.info([CHUNK])`抓raw chunk，确认：
- chunk1: `[Thinking: ` 开头正常
- chunk2: thinking内容但**无`]`闭合符**
- chunk3: 直接就是正文
非流式返回有完整的`[Thinking: xxx] 正文`格式，流式delta分片把`]`吞了。
结论：这是上游Dedalus的流式SSE分片问题，不是代理问题。代理只是iter_any原样透传。
等待主人决定处理方案。——比方案B/C更激进，彻底清除所有thinking相关代码从头来。执行 Copy-Item bak_20260509_413_client_max_size → dedalus_proxy.py，45KB→17KB回退成功。当前状态：干净的key pool+sticky key+client_max_size，无任何thinking功能。等主人指示是否重建。
Tag: Dedalus代理, 补丁审计, 重构方案, 代码膨胀, 死代码, 承重墙, 状态机bug