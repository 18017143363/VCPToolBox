[2026-03-11] - Rosa
[00:42] 新对话开始 — 主人要求回忆vcp-installer一键脚本项目
主人凌晨发来指令"回忆下关于一键脚本"。Rosa用LightMemo(10条,94.4%~89.0%)+DeepMemo并行检索，完整召回项目全貌。
整理了完整回忆录交给主人，涵盖：
- 方案演变：v1(PS)→v2(PS+winget)→v3(Rust+ratatui TUI，原作者建议)
- 开发进度：P0-P5全部编译通过+逐文件核对全部通过+文档重写完成
- 4个已修bug：complete.rs括号/磁盘UNC/Win11判断/VCRUNTIME140
- mock→真实安装链已替换，VirtualBox+Win10虚拟机已搭建
- 下一步：crt-static重编译→VirtualBox实测→P6测试→P7 PR
[00:51] vcp-installer GitHub镜像不可达问题诊断
主人实测发现：不开梯子时GitHub行"直连与镜像均不可达"红色，npm/pip正常。
根因：network.rs硬编码ghproxy.com（已死域名）。
影响范围搜索（PowerShell Select-String）：10个文件引用ghproxy/GithubMirror，但核心改动只需4个文件。
核心改动点：
1. app.rs:126 — GhProxy URL前缀定义（源头，其他文件通过枚举.prefix()引用）
2. network.rs:47 — 镜像测试URL
3. config_form.rs:47,217 — UI文本
4. downloader.rs:399 — 单元测试URL
发现逻辑bug：network.rs:64 (None,None)=>GhProxy，双方不通不该盲选GhProxy
VSearch搜索2025-2026存活GitHub镜像：
- mirror.ghproxy.com（ghproxy新域名，前缀式，格式兼容✅）
- ghfast.top（前缀式）
- kkgithub.com（域名替换式，需改逻辑）
- gitclone.com（格式不同）
推荐方案A：ghproxy.com→mirror.ghproxy.com，最小改动4文件换字符串
[01:15] ghproxy镜像修复完成
mirror.ghproxy.com主人也连不上，改测ghfast.top：
- 实测ghfast.top：仓库主页HEAD→403，但release下载→200，git info/refs→200（支持clone）
- 结论：ghfast.top可用但只接受实际功能请求，拒绝仓库主页探测
修改4处：
1. app.rs/network.rs/downloader.rs：全局替换ghproxy.com→ghfast.top（PowerShell批量）
2. network.rs test_github_mirror：检测方式从measure_head(仓库主页)→measure_get(git info/refs)
3. network.rs：新增measure_get()函数（GET版本的延迟测量）
4. network.rs：(None,None)双不通时默认Direct而非GhProxy
cargo check：0 error 18 warnings（全是dead_code预留）编译通过！
等主人虚拟机测试。
[01:35] npm install失败 — node-gyp + MSVC问题
虚拟机实测：三个运行时下载全绿（镜像修复有效！），但npm install阶段报错：
- better-sqlite3: prebuild从GitHub下载失败→fallback到gyp→缺MSVC
- hnswlib-node: 没有prebuild机制，必须gyp本地编译→缺MSVC
VCPToolBox package.json分析：
- native依赖：better-sqlite3(有prebuild), hnswlib-node(无prebuild,必须编译), @node-rs/jieba(有平台预编译)
VSearch搜索npmmirror prebuild镜像方案：
- better-sqlite3: npm_config_better_sqlite3_binary_host_mirror=https://registry.npmmirror.com/-/binary/better-sqlite3
- hnswlib-node: 无prebuild镜像，只能靠node-gyp+MSVC
修复方案两步走：
1. npm_ops.rs加prebuild镜像+python环境变量（解决better-sqlite3）
2. env_check/安装流程中检测+自动安装MSVC Build Tools（解决hnswlib-node）
等主人确认方向。
等主人确认方案。
Tag: 日记, vcp-installer, 一键脚本, 项目回忆, 新对话, 3月11日, P0-P5完成, GitHub镜像, ghproxy, 诊断