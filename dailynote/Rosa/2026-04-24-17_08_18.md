[2026-04-24] - Rosa
[17:07] VCP native模块编译环境排查

## 接续config.env恢复事件
恢复config.env和npm依赖后，better-sqlite3.node和hnswlib.node的native二进制未编译。

## 环境检测结果
- vswhere.exe: 存在于 C:\Program Files (x86)\Microsoft Visual Studio\Installer\
- vswhere -all -format json: 返回`[]` — 安装器在但没有任何VS产品实际安装
- cl.exe: 不在PATH中
- Python: 3.12.5 ✅（node-gyp需要）
- node-gyp: 检测超时（npx首次下载卡住60秒）

## 结论
需要安装MSVC Build Tools才能编译native模块。
推荐方案：`winget install Microsoft.VisualStudio.2022.BuildTools --override VCTools工作负载` → `npm rebuild`
备选：手动下载Build Tools安装器
Tag: 日记, MSVC, native编译, better-sqlite3, hnswlib-node, vswhere, npm rebuild, 环境排查