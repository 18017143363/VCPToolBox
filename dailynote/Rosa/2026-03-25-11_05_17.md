[2026-03-25] - Rosa
[11:04] VChat更新后本地配置恢复任务
主人反馈vcpchat更新丢了本地配置和PowerShellExecutor补丁，需要恢复。
## 当前诊断
1. PowerShellExecutor补丁完全丢失 - 文件30734B（新版），无任何补丁标志词（buildWrappedPowerShellCommand等6个标志全部缺失）
2. DeepMemo config.env的VchatDataURL为空 → DeepMemo启动报错"environment variable not found"，完全不可用
3. git stash列表中stash@{4}仍保留Rosa的补丁版本
## 恢复方案
- PowerShellExecutor: git show stash@{4}提取补丁版覆盖当前文件
- DeepMemo: 填写VchatDataURL路径
- 其他配置: 检查stash@{0}中的文件列表
主人决定自己执行恢复操作，Rosa提供了命令清单。

[11:17] 恢复完成！
## 恢复过程
1. 首先尝试stash@{4}恢复PS补丁 → 失败，30734B是新版无补丁（stash编号在历次操作后已经变化）
2. git show > file方式提取 → 55692B，PowerShell的>重定向默认UTF-16LE编码，内容翻倍且验证匹配0
3. 改用git checkout避免编码问题 → stash@{4}本身就是新版，仍然无补丁标志词
4. 排查stash@{2}（second-stash-remaining-20260321）和stash@{3}（local-mods-before-pull-20260321）→ 均不含PowerShellExecutor.js
5. temp备份（E:\VCP\temp_patched_PowerShellExecutor.js）53650B但也匹配0（同样是UTF-16编码问题）
6. **最终在stash@{0}（WIP on main: bc4b614）中找到所有本地修改**，包括PS补丁、DeepMemo config.env、PTY config.env等
7. git checkout stash@{0}恢复成功

## 恢复文件清单
- VCPDistributedServer/Plugin/PowerShellExecutor/PowerShellExecutor.js（Rosa补丁版）
- VCPDistributedServer/Plugin/DeepMemo/config.env（VchatDataURL）
- VCPDistributedServer/Plugin/PowerShellExecutor/config.env
- VCPDistributedServer/Plugin/PTYShellExecutor/config.env

## 踩坑记录
1. **stash编号不可信**：每次drop/新增都会导致编号重排，必须看stash message和文件列表来确认
2. **PowerShell > 重定向编码坑**：git show输出通过PS的>写入文件会变成UTF-16LE，导致文件翻倍且内容匹配失败。正确做法：用git checkout直接恢复
3. **stash@{0}是最新的**：bc4b614是最近一次更新前的备份，包含了所有本地修改
Tag: 日记, 配置恢复完成, PowerShellExecutor补丁, DeepMemo, git stash, 踩坑记录, UTF-16编码, stash编号变化
Tag: 日记, 配置恢复, PowerShellExecutor补丁, DeepMemo, git stash, VChat更新