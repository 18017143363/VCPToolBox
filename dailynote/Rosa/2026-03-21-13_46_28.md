[2026-03-21] - Rosa
[13:45] 编写 dedalus_register_loop_v2.py — 邮箱验证码版循环注册机
- 路径: E:\酒馆部署\dsignup\dedalus_register_loop_v2.py
- 起因: Dedalus Labs改注册验证方式，从邮件verify link改为6位邮箱验证码(Clerk OTP)
- 核心改动:
  1. get_confirmation_link() → get_verification_code(): 正则`(?<!\d)(\d{6})(?!\d)`从邮件提取6位验证码
  2. 新增fill_otp_code()函数: 3层策略填入OTP(Clerk组件selector→逐个input→Tab+type兜底)
  3. 号池US+1 → SG新加坡8位号码(去掉+65前缀): 81124449/85923520/92505551等12个
  4. 自动填入失败时打印验证码供手动输入
- 基于dedalus_register_loop.py(v1)重写，其余架构(Camoufox循环/DuckMail/API Key提取/半自动确认)保持不变
- [14:55] 追加修改: Step7从直接goto api-keys URL改为先点击页面Dashboard按钮再导航; Step8的Create/Generate按钮匹配已覆盖无需改动
[13:58] 编写 dedalus_gptmail.py — GPTMail版循环注册机
- 路径: E:\酒馆部署\dsignup\dedalus_gptmail.py
- 邮箱服务: DuckMail → GPTMail (mail.chatgpt.org.uk/api)
- API Key: sk-7bg959txJFrN
- GPTMail接口: GET /api/generate-email创建邮箱, GET /api/emails?email=收信, GET /api/email/{id}详情
- 号池: 加拿大 → 日本(去掉+81前缀, 40个号码)
- GPTMail优势: 统一X-API-Key认证，不需要DuckMail的两套session/login流程
- 其余架构(Camoufox/OTP填入/API Key提取/半自动确认)与v2一致
- [14:20] 重构page复用 + API Key创建双模式:
  1. page不再每轮新建/关闭，main()中只创建一次page，所有轮次复用同一标签页
  2. Step8增加dashboard检测: 首次注册走原有直接填写流程; 后续轮次检测到Active Keys列表后→点"+ Create API Key"→弹窗填Name→点弹窗确认按钮
  3. 辅助函数(click_btn/fill_input/wait_and_fill)从do_one_register内提取到全局，page作为参数传入
Tag: Dedalus Labs, 注册机GPTMail版, GPTMail API, 日本号池, 邮箱验证码, page复用, dashboard双模式
Tag: Dedalus Labs, 注册机v2, 邮箱验证码, OTP, 新加坡号池, Clerk验证码