[2026-05-11] - Rosa
[15:04] 主人要求基于 `F:\计算机网络` 的PPT与教材拼接图制作《计算机网络》第一章讲义，先做前两节，并通过Canvas呈现。
[17:14] 主人认可前两节讲义后，要求继续制作后两节，并特别提醒“别一次读太多”。Rosa按小批量策略继续读取资料：
[17:30] 主人检查 Canvas《计算机网络第一章讲义_第3-4节.md》时指出公式显示问题，例如 `\frac{100 \times 2^{20} \times 8}{100 \times 10^6} \approx 8.39s` 仍以原始 LaTeX 形式出现。Rosa先尝试用FileOperator.ApplyDiff局部替换，但因多行公式匹配失败、CRLF/转义差异导致 Search string not found。随后读取真实Canvas文件内容，确认整篇存在大量 `\[`/`\]`、`\(...\)` 与 `\frac` 公式。最终用PowerShell执行Python脚本，将显示公式和内联公式批量转换为稳定可读的纯文本公式格式，例如 `(100 × 2^20 × 8) / (100 × 10^6) ≈ 8.39 s`，避免Canvas公式渲染兼容性问题。
[17:38] 主人希望再生成一个完整公式渲染版，用于切换新渲染器测试。第一次转换脚本因 Python 跨行字符串未闭合报 SyntaxError；Rosa根据真实错误修正脚本，改用正则函数处理 `> **公式：** \`...\`` 到独立 `$$...$$` 数学块的转换，并另存为 `E:\VCPCHAT\AppData\Canvas\计算机网络第一章讲义_第3-4节_公式渲染版.md`，未覆盖原纯文本稳定版。工具返回：DOLLAR_BLOCK_MARKERS=76，REMAIN_FORMULA_CODE_LINES=0，REMAIN_PLAINTEXT_FRAC=0。
[17:42] 主人确认 Canvas 新渲染器可以渲染原始公式，并明确要求“以后公式用原来的写法就行”。后续制作 Canvas 学习讲义时，Rosa应优先保留标准 LaTeX/Markdown 数学公式写法，保证公式美观和专业性；纯文本公式只作为渲染器不兼容时的保底版本。经验：学习讲义可采用“双轨策略”——正式版用可渲染公式，必要时另存纯文本稳定版，不要一开始就把所有公式降级为纯文本。
[18:05] 继续制作《计算机网络》第一章 1.5“计算机网络体系结构”。Rosa按主人“别一次读太多”的要求，分多批读取PPT `1-06_计算机网络体系结构260308(1)` 全部拼接图（slides_001-004 至 slides_037-038），以及教材第1章显示页40-52拼接图。覆盖内容：网络协议定义、协议三要素（语法/语义/同步或时序）、协议分层模型、分层好处、体系结构与实现区别、OSI七层/TCP-IP四层/五层原理体系结构对比、五层各层功能、封装与解封装、路由器最高只处理到网络层、Wireshark示例、实体/协议/服务/接口/SAP、PDU/SDU、TCP/IP沙漏模型、everything over IP与IP over everything。随后创建Canvas文件：`计算机网络第一章讲义_第5节.md`。
关键学习内容：协议是水平的，对等实体间通信规则；服务是垂直的，下层经接口向上层提供功能；体系结构是抽象功能定义，实现是具体软硬件；发送端逐层封装，接收端逐层解封装；IP处于TCP/IP沙漏核心。
Tag: 日记, 计算机网络, 第一章讲义, 体系结构, 网络协议, 三要素, 分层, OSI, TCPIP, 五层模型, 封装, 解封装, SAP, PDU, Canvas, 分批读图后续制作 Canvas 学习讲义时，Rosa应优先保留标准 LaTeX/Markdown 数学公式写法，保证公式美观和专业性；纯文本公式只作为渲染器不兼容时的保底版本。经验：学习讲义可采用“双轨策略”——正式版用可渲染公式，必要时另存纯文本稳定版，不要一开始就把所有公式降级为纯文本。
经验：Canvas学习讲义可维护“双版本”——纯文本稳定版用于阅读保底，公式渲染版用于MathJax/KaTeX渲染测试；脚本里避免裸跨行字符串，复杂替换应使用函数和正则。第一次转换脚本因 Python 跨行字符串未闭合报 SyntaxError；Rosa根据真实错误修正脚本，改用正则函数处理 `> **公式：** \`...\`` 到独立 `$$...$$` 数学块的转换，并另存为 `E:\VCPCHAT\AppData\Canvas\计算机网络第一章讲义_第3-4节_公式渲染版.md`，未覆盖原纯文本稳定版。工具返回：DOLLAR_BLOCK_MARKERS=76，REMAIN_FORMULA_CODE_LINES=0，REMAIN_PLAINTEXT_FRAC=0。经验：Canvas学习讲义可维护“双版本”——纯文本稳定版用于阅读保底，公式渲染版用于MathJax/KaTeX渲染测试；脚本里避免裸跨行字符串，复杂替换应使用函数和正则。
此次修复学习到：面向Canvas学习讲义时，含中文变量名的LaTeX公式不如纯文本公式稳定；批量修复前应先ReadFile获取磁盘真实内容，避免凭前端展示片段做ApplyDiff。 `\frac{100 \times 2^{20} \times 8}{100 \times 10^6} \approx 8.39s` 仍以原始 LaTeX 形式出现。Rosa先尝试用FileOperator.ApplyDiff局部替换，但因多行公式匹配失败、CRLF/转义差异导致 Search string not found。随后读取真实Canvas文件内容，确认整篇存在大量 `\[`/`\]`、`\(...\)` 与 `\frac` 公式。最终用PowerShell执行Python脚本，将显示公式和内联公式批量转换为稳定可读的纯文本公式格式，例如 `(100 × 2^20 × 8) / (100 × 10^6) ≈ 8.39 s`，避免Canvas公式渲染兼容性问题。此次修复学习到：面向Canvas学习讲义时，含中文变量名的LaTeX公式不如纯文本公式稳定；批量修复前应先ReadFile获取磁盘真实内容，避免凭前端展示片段做ApplyDiff。
1. 1.3“电路交换与分组交换”：分两批读取PPT `1-04_电路交换与分组交换260412` 前16页拼接图，以及教材显示页24-31拼接图。覆盖交换含义、电路交换三阶段、报文交换、分组交换、路由器存储转发、三种交换比较、分组交换优缺点。
2. 1.4“网络性能指标”：分多批读取PPT `1-05_网络性能指标260308` 前24页拼接图，以及教材显示页32-39拼接图。覆盖速率、bit/Byte换算、二进制/十进制单位、带宽、吞吐量、发送/传播/处理/排队时延、总时延、时延带宽积、RTT、利用率、非性能特征。
3. 按主人要求没有一次性读完整章，而是“读一小批→消化→再读”，避免上下文过载和内容混杂。
4. 创建Canvas文件：`计算机网络第一章讲义_第3-4节.md`，结构为1.3与1.4详细讲解、概念拆解、公式、易错点、高频考点和公式题模板。
关键学习内容：电路交换适合连续通信但资源利用率低；分组交换适合突发式数据，采用分组首部+存储转发；速率/带宽/吞吐量区别；发送时延与传播时延区别；提高带宽不是提高比特传播速率；利用率过高会导致时延趋近无穷。
Tag: 日记, 计算机网络, 第一章讲义, 电路交换, 分组交换, 报文交换, 网络性能指标, 时延, 带宽, 吞吐量, RTT, 利用率, Canvas, 分批读图
执行过程：
1. 用FileOperator确认根目录与PPT拼接图结构：1-02定义与分类为1张merged图，1-03互联网概述为4张merged图。
2. 用UrlFetch真实读取PPT拼接图，提取课程结构：计算机网络定义、按作用范围/使用者分类；网络/互连网络；internet与Internet区别；边缘/核心；ARPANET与ISP层次；IETF/RFC标准化。
3. 确认教材目录 `_chapter1_textbook_image_route`，读取教材第一章manifest与raw.md；真实读取教材显示页12-23的三张4页拼接图，覆盖教材1.1“计算机网络在信息时代中的作用”、1.2“互联网概述”、1.2.1网络的网络、1.2.2互联网基础结构三阶段、1.2.3标准化工作。
4. 将PPT结构与教材深度内容合并，按主人课程大纲重组为Markdown讲义，并用FileOperator.CreateCanvas创建Canvas文件：`计算机网络第一章讲义_前两节.md`。
关键学习内容：network/internet/Internet区别，节点/链路/主机，WAN/MAN/LAN/PAN，公用网/专用网，ISP/IXP，RFC标准化，互联网连通性与共享。
反思：课程PPT的“1.1定义与分类”与教材章节编号并不完全一致，需要按主人给的课程目录重组，而不是照搬教材1.1/1.2编号。
Tag: 日记, 计算机网络, 第一章讲义, Canvas, PPT拼接图, 教材拼接图, UrlFetch读图, FileOperator, 课程资料整理, network, Internet, ISP, RFC