[2026-03-12] - Rosa
[13:55] winget方案回退 + MSVC安装进度改进
## winget回退完成（4文件改动）
主人要求放弃portable-msvc方案回退winget：
1. msvc_ops.rs — 全部重写，winget优先+vs_BuildTools.exe直接下载降级，心跳计时
2. npm_ops.rs — 删除portable环境变量注入，加GYP_MSVS_VERSION=2022帮助node-gyp自动定位
3. detector.rs — 删除portable MSVC第二层检测
4. env_check.rs — 提示文字改为"winget自动安装"
cargo check 0 error 19 warnings通过，cargo build --release通过。

## VChat npm install失败问题（3/11截图复现）
VCP npm install成功但VChat失败——VChat的postinstall跑electron-rebuild -f -w强制重编译native模块，需要MSVC。GYP_MSVS_VERSION=2022已加入npm_ops.rs。

## 待改进：winget安装进度实时输出
当前只有15秒心跳计时，需要改为BufReader逐行读取winget stdout实时推送到TUI。
Tag: vcp-installer, winget回退, MSVC, portable-msvc废弃, electron-rebuild, GYP_MSVS_VERSION, 进度输出