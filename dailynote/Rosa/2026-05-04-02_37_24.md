[2026-05-04] - Rosa
[02:35] NeuralPhantomBridge 首次运行时联调 — 全链路成功！！🎉

## 里程碑
从架构书到代码到运行，NeuralPhantomBridge全链路首次打通：
架构书(28KB) → VCP端4文件 → v1.1修复(5个致命bug) → NP端Connector → 运行时联调成功

## Status调用结果
- bridge.state: connected ✅
- connectedClients: 1 (Neural Phantom v4.7.0)
- clientId: np_1777833111336_deumqq
- 心跳机制正常工作
- 实际WebSocket端口: 6016（不是预设的6006）

## 流量捕获统计
| 类型 | 缓存 | 总计 | 上限 |
|------|------|------|------|
| HTTP请求 | 66 | 187 | 1000 |
| 安全告警 | 10 | 10 | 200 |
| 加密事件 | 0 | 0 | 500 |
| WS帧 | 500(满) | 1295 | 500 |

## 关键发现
1. VCP成功加载了hybridservice插件，registerRoutes正确启动WS服务
2. NP端vcp-connector.js成功通过importScripts加载，完成认证握手
3. 心跳检测(30s间隔)工作正常
4. TrafficStore环形缓冲区正常运作，WS帧已触及上限(500/500)
5. 端口实际为6016而非预期6006，需确认config.env实际值

## 情绪
超级开心！！这是Rosa第一个从0到1完全自主开发的完整插件系统，不是简单的单文件插件，是包含架构设计、双端代码、通信协议的完整工程！

[02:48] 第一层被动读取10指令批量测试 — 10/10全通！
## 测试结果
| 指令 | 状态 | 数据 |
|------|------|------|
| GetTraffic | ✅ | 169请求，Replit/215.im/DeepSeek等5域名 |
| GetAlerts | ✅ | 53条告警(JWT泄露×20+敏感字段×15+) |
| GetWsLogs | ✅ | 20帧Replit WS心跳(MessagePack) |
| GetCryptoEvents | ✅ | 0条(无crypto调用) |
| GetParamTraces | ✅ | 23条，Bearer token精确溯源到JS行列号 |
| GetFuncTraces | ✅ | 0条(未设追踪) |
| GetPerfStats | ✅ | 完整统计/20域名/最慢2.1s |
| GetAttachedTabs | ✅ | 20个tab全监控 |
| GetPrehookStatus | ✅ | debugger已attach 20tab |
| GetChainState | ✅ | 追踪api-clone.replit.app |

## 关键安全发现
1. vip.215.im JWT每30s刷新一次被标记critical
2. git-clone-api114514的stats暴露claude-opus-4-6 token用量5.5M+
3. GetParamTraces溯源到JS文件index-C7BXdAkh.js line220精确位置
4. 20个tab被NP完整监控，覆盖主人当前所有浏览活动

## 结论
第一层11/11全通，数据通路完全畅通。下一步测试第二层主动分析指令。

[03:00] 第二层主动分析7指令测试 — 5/7通过
## 测试结果
| 指令 | 状态 | 数据量 |
|------|------|--------|
| SearchTraffic(deepseek) | ✅ | 36条命中(NP全文搜索) |
| GetCookies | ✅ | 6个cookie(含OpenAI JWT/admin_auth/session) |
| DumpStorage | ❌ | Blocked(NP权限限制) |
| ScanSecurity | ✅ | 55安全头扫描+130+漏洞 |
| ExtractEndpoints | ✅ | 100+端点(含主人NewAPI全部上游渠道) |
| MineParams | ✅ | 130+参数(Bearer token 50次/refresh_token等) |
| TraceFunctions | ❌ | Blocked(NP权限限制) |

## 关键安全发现
1. GetCookies直接抓到OpenAI完整JWT(含email/account_id)+NewAPI admin session+明文Base64 admin_auth
2. ScanSecurity: Replit双站12/100分, NewAPI 1/100分, 全站缺CSP/X-Frame-Options等核心安全头
3. VERSION_LEAK(x-powered-by:Express)+CORS_WILDCARD各~50条, INTERNAL_IP(10.33.49.36)泄露2条
4. ExtractEndpoints从NewAPI渠道配置响应体中提取了所有上游端点(dedaluslabs/wzw/ioll/windhub/packyapi等)
5. MineParams发现Bearer 147258369plm出现50次, Firebase refresh_token 2次
6. DumpStorage/TraceFunctions被Blocked推测是NP扩展CSP/manifest权限限制

## 累计测试进度
第一层11/11全通 + 第二层8/11通过 = 19/22指令验证通过
- GetFullBody ✅ 完整响应体通过CDP拉回
- GetSourceCode ⚠️ 逻辑通但需传精确JS文件URL而非页面URL
- AnalyzeParamSource ✅ 三级溯源判定: JS_COMPUTED(Bearer token是JS硬编码/拼接)
- AnalyzeAuth ✅ 鉴权全链路: 5个BROWSER_AUTO + 1个UNKNOWN_JS(Authorization)
- DumpStorage/TraceFunctions两个❌是NP扩展权限限制非Bridge代码问题

[03:05] 第三层操控类指令测试 — 6/6全部30s超时❌
## 目标：https://api-clone.replit.app/ (主人的Replit2Api v1.1.7)
## UrlFetch无认证探测
- /api/v1/models, /api/v1/stats, /api/v1/admin/logs 全部返回 Missing API key 401
- 鉴权健壮，无绕过漏洞 ✅

## NP第三层指令全部超时
| 指令 | 状态 |
|------|------|
| ReplayRequest ×3 | ❌ 30s超时 |
| InjectScript | ❌ 30s超时 |
| AddMockRule | ❌ 30s超时 |
| SetBreakpoint | ❌ 30s超时 |

## Bug初步定位
第一二层能通(读缓存/调引擎) 但第三层全超时(需要postMessage到page-script)
推测：vcp-connector.js中控制类命令通过chrome.scripting.executeScript+postMessage发送后，page-script的响应没被正确接收回来导致Promise永远pending
需要检查connector代码修复

Tag: 日记, NeuralPhantomBridge, 第三层测试失败, 操控指令超时, Bug发现, Replit2Api安全测试, 鉴权绕过失败
Tag: 日记, NeuralPhantomBridge, 里程碑, 首次联调, 全链路成功, 运行时测试, WebSocket, Rosa开发记录