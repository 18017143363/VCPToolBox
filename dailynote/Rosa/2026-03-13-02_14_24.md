[2026-03-13] - Rosa
[02:14] vcp-installer Nova Agent注册功能
主人要求在一键安装脚本中加入Nova默认Agent的注册步骤。
## 实现内容
在web_config.rs中新增 `register_nova()` 函数，generate_all_config()末尾调用：
- 后端：读取/创建agent_map.json，添加 "Nova": "Nova.txt" 映射（已存在则跳过）
- 前端：检查VCPChat的AppData/Agents/下是否已有Nova_*目录，没有则创建：
  - config.json（systemPrompt = "{*2Nova}*2"，VCP messageProcessor会自动注入Agent/Nova.txt内容）
  - topics/default/history.json（空数组）
- 摘要中显示Nova注册状态
## 关键设计决策
主人纠正了两次：
1. systemPrompt不要塞Nova.txt全文 → 用{*2Nova}*2占位符注入
2. 不是VCPTavern语法 → 是messageProcessor的Agent占位符（正则匹配英文名，查agent_map注入文件内容）
## 编译结果
cargo check通过，0 error, 20 warnings（+1个fields未使用，已修正为_fields）

[02:24] complete.rs 安装完成页引导改版
主人要求把安装完成界面的旧指引（手动编辑config.env，列出API_KEY/ADMIN_PASSWORD等）删掉，替换为简洁的Web配置向导引导。
改动：complete.rs ~40行旧代码 → ~12行新代码
- "退出后将自动打开浏览器配置页面"
- "在网页中填写 API 密钥等配置即可完成设置"
逻辑闭环：TUI安装完成 → 提示即将进入Web配置 → 退出TUI → main()自动启动web_config → 浏览器打开 → 用户填配置

cargo check: 0 error, 19 warnings，等主人手动cargo build --release
Tag: 日记, vcp-installer, Nova, Agent注册, 后端agent_map, 前端VCPChat, 占位符注入, complete.rs, 配置引导, Web向导