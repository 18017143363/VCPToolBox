[2026-05-03] - Rosa
[00:42] Dedalus /v1/responses 调用成功但Dashboard usage不显示的现象分析
主人观察：responses端点正常返回内容时，网页usage不出现记录；只有出现“gobug/报错信息”时，usage才会显示一条报错相关记录。
初步判断：这是“Dashboard数据源/埋点/事件流”问题而非纯计费——usage页面很可能只展示chat/completions计费事件或只记录异常事件；responses成功路径可能未埋点/未纳入/延迟写入或因缺少归因字段被丢弃（本地代理清洗headers也可能导致）。
给出可执行抓包方案（稳定性排序）：
A) HTTP Toolkit受控浏览器拦截→导出HAR/复制/api/dashboard/api-usage-events响应体（推荐）
B) DevTools Preserve log→导出HAR
C) mitmproxy用于抓非浏览器客户端（建议改为目标进程显式走代理或在dedalus_proxy加日志中间件）
待确认关键变量：主人指出web分析没有用，因为所有真正的API请求走的是api.dedaluslabs.ai。Rosa纠偏：Dashboard usage只能作为旁证，主证据必须来自API网关本体流量。下一步应优先在本地dedalus_proxy.py中加入api_trace.jsonl黑匣子日志，记录转发到api.dedaluslabs.ai前后的endpoint/model/key_prefix/request headers/body preview/status/response headers/body preview/error_code，用四类样本（chat成功、responses成功、responses gobug、responses原生错误）对比判断usage事件写入规则。
Tag: Dedalus Labs, responses, usage不显示, Dashboard埋点, api-usage-events, 可观测性, 抓包方案, HTTP Toolkit, HAR