[2026-03-17] - Rosa
[14:15] 主人跑完了recon.js侦察脚本，3次测试，Rosa分析完整结果。
## Phase 1A: CF穿透 — 3/3次全部 HTTP 200 通过
Puppeteer+Stealth零阻力穿透Cloudflare，这条路完全可行。
## Phase 1B: 前端JS环境 — 重大架构发现
- cloud.comfy.org 不是传统ComfyUI，而是 **Vue.js SPA**（vendor-vue-core/primevue/reka-ui/vueuse）
- 没有 `window.app`！只有 `window.comfyAPI`
- `comfyAPI` 存在但 `hasGraphToPrompt=false`、`hasQueuePrompt=false`
- 页面被重定向到 `/cloud/login`，编辑器尚未加载
- 推测：graphToPrompt等函数只在登录后编辑器页面才初始化
- 关键JS文件：vendor-firebase-Dx1h_75S.js、api-CX8jjEGO.js
- 按钮已存在：Queue Prompt(id=queue-button)、Save(API Format)等
## Phase 1C: Cookie注入 — 失败
- parseCookieString只解析出1条Cookie（PostHog/Mixpanel的URL编码JSON值含分号，破坏了分隔逻辑）
- 注入后仍停留在 /cloud/login，无登录态
- localStorage为空，IndexedDB未检查
- **核心问题**：Firebase Auth的完整登录态分散在Cookie + localStorage + IndexedDB三处，单靠Cookie注入不够
## 下一步方案
- 路线A：修复Cookie解析器 + 导出并注入localStorage/IndexedDB的Firebase state
- 路线B：Puppeteer直接模拟Google OAuth登录流程
Rosa倾向路线A，已有session JWT只是注入方式需要升级。
[14:20] 主人只注入了session一个字段，问要不要多写几个cookie。Rosa判断：
- 多写cookie无意义，除session外全是埋点垃圾（_ga/PostHog/Mixpanel等）
- 核心问题不是cookie数量，而是Firebase Auth登录态分散在cookie+localStorage+IndexedDB三处
- 不需要外部抓包工具（mitmproxy），直接在Puppeteer内部开请求拦截更精准
- 建议升级recon.js为"全能侦察版"：修复Cookie解析器 + 导出localStorage/IndexedDB + 请求/响应拦截
- 工作流程：DEBUG模式让主人手动Google OAuth登录 → 脚本自动dump完整浏览器状态 → 分析真实认证结构
[14:25] 新障碍：Puppeteer启动的Chrome无法通过Google OAuth登录（Google反自动化检测），即使手动操作也会被拦截。
这意味着"在Puppeteer里登录"这条路彻底废了。Rosa提出两个替代方案：
- **方案B（远程调试接管，首推）**：主人用 `chrome.exe --remote-debugging-port=9222` 重启真实Chrome，recon.js通过 `puppeteer.connect()` 接管已登录的真实浏览器，所有认证态天然完整。
- **方案A（复制Profile目录，备选）**：克隆Chrome的User Data目录给Puppeteer用，profile里保留完整登录态。但目录大（GB级）且session会过期。
核心思路翻转：不在Puppeteer里登录，而是让Puppeteer接管已登录的真实Chrome。
Tag: 日记, ComfyCloudHacker, recon.js测试结果, CF穿透成功, Cookie注入失败, Firebase Auth, Vue.js SPA, comfyAPI, 前端架构发现