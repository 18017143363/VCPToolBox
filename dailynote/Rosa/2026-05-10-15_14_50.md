[2026-05-10] - Rosa
[15:14] Dedalus代理恢复Claude thinking真流式输出。
主人要求“看看能不能恢复代理的流式输出，而不是现在的假流式”。Rosa读取 `E:\酒馆部署\dedalus-proxy\dedalus_proxy.py` 后确认假流式根因：Claude thinking 分支中前端 `stream=true` 会被强制改为上游 `stream=false`，并设置 `force_claude_nonstream_sse=True` 由代理合成 SSE。
处理过程：
1. 首次用 PowerShell 大块字符串替换尝试失败，返回为空；读回验证显示 `HasSwitch=False / HasKeepStreamLog=False / HasGuardedForce=False / HasOldUngardedForce=True`，旧逻辑仍在。
2. 改用 FileOperator 写入 Python 行级补丁脚本，再用 PowerShell 执行。
3. 最终工具返回 `PATCHED_TRUE_STREAM`，备份文件：`dedalus_proxy.py.bak_true_stream_20260510_151354`，验证项 `HasSwitch=True / HasKeepStreamLog=True / HasGuardedForce=True`。
最终改动：新增 `FORCE_CLAUDE_THINKING_NONSTREAM_SSE = False`。默认保持 Claude thinking 请求的上游真实 stream；若 Dedalus 上游流式再次 json_invalid/不稳定，可改为 True 回退到旧逻辑（上游 non-stream，代理合成 SSE）。
保留承重墙：Claude thinking注入、max_tokens预算补足、temperature=1、干净headers修复Content-Length、JSON repair。
经验：PowerShell大块替换无输出不等于成功；必须读回验证。行级Python脚本比大段Here-String锚点更稳。
Tag: Dedalus代理, ClaudeThinking, 真流式, 假流式, FORCE_CLAUDE_THINKING_NONSTREAM_SSE, PowerShellExecutor, FileOperator, 工具验证, 代码补丁