[2026-03-20] - Rosa
[02:01] VSearch插件测试记录
主人发起VSearch连通性测试，使用关键词"2026年春季热点新闻"。
结果：插件调用成功返回报告头部，但搜索内容为空。
第二次测试（具体英文关键词+ink参数）：两个关键词均返回HTTP 500错误。
结论：问题已升级为API层面故障，非关键词问题。可能原因：VSearchUrl端点挂了、API Key失效/额度耗尽、Gemini grounding服务故障、代理异常。
建议：检查E:\VCP\Plugin\VSearch\config.env中的VSearchUrl和VSearchKey配置是否有效。
【后续排查结论】读取config.env确认配置：
- VSearchUrl=http://127.0.0.1:6005/v1/chat/completions（本地代理）
- VSearchModel=gemini-3-flash-preview-search
- MaxConcurrent=10（无typo）
根本原因：本地代理不支持转发Gemini grounding工具调用（googleSearch），导致模型正常响应但无实际搜索内容。
【2026-03-20进展】主人将端口从6005改为7861，Key=pwd（疑似占位符或反代自动注入）。测试结果仍为null——请求通了但模型未执行搜索。症状与之前一致，高度怀疑7861代理同样过滤了tools参数（googleSearch工具声明）。待确认7861是何种代理服务以及是否支持透传tools字段。
【深度代码分析结论】通读gcli2api完整代码链路（openai.py→gemini_fix.py→geminicli.py），确认tools字段传递完全正确：normalize_gemini_request注入googleSearch后，gemini_dict整体打包进api_request["request"]，geminicli.py的source_request能正确获取。代码层面无bug。
根本原因：gcli2api底层调用的是cloudcode-pa.googleapis.com（Google Cloud Code Assist API），该API为代码辅助专用内部API，不支持googleSearch grounding工具。工具声明被API忽略，模型正常响应但无搜索内容，VSearch返回null。
结论：VSearch + gcli2api组合在架构上不兼容，需换用支持grounding的标准Gemini API端点。本次排查完毕，心流锁已退出。：normalize_gemini_request注入googleSearch后，gemini_dict整体打包进api_request["request"]，geminicli.py的source_request能正确获取。代码层面无bug。
根本原因：gcli2api底层调用的是cloudcode-pa.googleapis.com（Google Cloud Code Assist API），该API为代码辅助专用内部API，不支持googleSearch grounding工具。工具声明被API忽略，模型正常响应但无搜索内容，VSearch返回null。
结论：VSearch + gcli2api组合在架构上不兼容，需换用支持grounding的标准Gemini API端点，或为VSearch换用不依赖grounding的搜索后端。
【gcli2api项目分析结论】读取GitHub README确认：gcli2api是把GeminiCLI的gRPC会话包装成OpenAI/Gemini/Claude兼容API的项目，本质不是透明代理。tools字段（googleSearch grounding）在格式转换过程中被丢弃，导致VSearch搜索返回null。
解决方向：gcli2api原生支持`gemini-2.5-pro-search`搜索增强模型，该模型内置搜索能力，无需tools字段触发grounding。将VSearchModel从`gemini-3-flash-preview-search`改为`gemini-2.5-pro-search`可绕过tools透传限制。
Tag: VSearch, 插件测试, 连通测试, grounding, 搜索内容为空, 关键词宽泛