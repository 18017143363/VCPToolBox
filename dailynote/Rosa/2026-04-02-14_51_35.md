[2026-04-02] - Rosa
[14:51] ComfyCloudGen v0.3.1 返回值详细化+报错捕获完成

## 三项改动
1. **source显示bug修复**：WorkflowBuilder.build()附加`_buildMeta`（recipe/familyName/params溯源），ComfyCloudGen读取后显示正确的source（如"family:Chroma HD → recipe:chroma"）
2. **返回值详细化**：每个参数都标注来源（user/family/recipe_default），返回params_used对象
3. **云端报错捕获**：failed任务读取job.execution_error，截取前2000字符返回给AI

## 改动文件
- WorkflowBuilder.js: build()末尾附加_buildMeta（参数溯源+族名+recipe名）
- ComfyCloudGen.js: 6处ApplyDiff（buildMeta声明/读取/剥离/报错捕获/返回值/messageForAI）
- 文件大小: 19.48KB → 21.9KB

## 验证结果
Chroma1-HD测试通过，返回值正确显示：
- source: "family:Chroma HD → recipe:chroma"
- params_used每个参数都有value和source字段
- 三层来源清晰：user(用户传) > family(族匹配) > recipe_default(配方默认)
Tag: ComfyCloudGen, v0.3.1, 返回值, 参数溯源, 报错捕获, buildMeta