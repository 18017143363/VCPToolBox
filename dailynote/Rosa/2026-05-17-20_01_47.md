[2026-05-17] - Rosa
[20:01] Dedalus代理Vercel版新增 `/v1/models` 模型列表接口并部署成功。
主人要求给Vercel代理加硬编码models列表，普通版和pro版都全加。Rosa新增 `api/v1/models.js`，返回OpenAI兼容 `{object:"list", data:[...]}`，并把 `vercel.json` 的 CORS methods 从 `POST, OPTIONS` 改为 `GET, POST, OPTIONS`。由于已有 rewrite `/v1/(.*)` → `/api/v1/$1`，线上 `/v1/models` 可直接命中新增函数。
已部署到 Vercel 项目 `tempproxy111`，部署输出 Ready in 15s。线上实测：
- `GET https://tempproxy111.vercel.app/v1/models` → HTTP 200，object=list，count=22
- `GET https://tempproxy111.vercel.app/api/v1/models` → HTTP 200
模型列表包含：gpt-5.4 六档、gpt-5.4-search 五档、gpt-5.4-pro 六档、gpt-5.4-pro-search 五档，共22个。
提醒：pro 的 none/low 虽按主人要求列出，但上游实际可能不支持，调用时会由Dedalus返回错误；模型列表功能本身已完成。当前Vercel代理支持 `/v1/chat/completions` 与 `/v1/models`，NewAPI可尝试自动拉取模型。
Tag: Rosa日记, Dedalus代理Vercel版, /v1/models, OpenAI模型列表, NewAPI自动拉模型, tempproxy111, Vercel部署, 2026-05-17