[2026-03-12] - Rosa
[15:58] MSB8040+LNK1181双杀修复
## 问题链
1. MSB8040 Spectre缓解库缺失 → Directory.Build.targets强制SpectreMitigation=false → 已解决
2. LNK1181找不到delayimp.lib → targets追加WindowsSDK lib路径 → 待虚拟机验证

## 修改文件
npm_ops.rs的targets_content从纯SpectreMitigation扩展为：
- PropertyGroup: SpectreMitigation=false（干掉MSB8040）
- ItemDefinitionGroup/Link/AdditionalLibraryDirectories: $(WindowsSdkDir)Lib\$(WindowsSDKLibVersion)um\$(Platform)（干掉LNK1181）

## 踩坑
1. SpectreMitigation环境变量对MSBuild无效，必须用targets文件
2. PowerShell Here-String替换Rust raw string literal失败（空返回），改用Node.js脚本
3. delayimp.lib是Windows SDK的lib，编译能过但链接找不到说明SDK headers在但lib搜索路径不对

## 方案演变
MSVC INSTALL_ARGS加Spectre组件(500MB) → SpectreMitigation环境变量(无效) → Directory.Build.targets(有效) → 追加SDK lib路径

cargo check 0 error 19 warnings通过，等build+虚拟机测试。

## 第二轮修复（虚拟机验证失败后）
MSBuild变量$(WindowsSdkDir)等在winget环境下解析为空，导致targets里的SDK路径无效。
最终方案——三重保险：
1. INSTALL_ARGS加Spectre组件（直接装库）
2. targets SpectreMitigation=false（跳过检查）
3. **Rust探测SDK绝对路径**：扫描`C:\Program Files (x86)\Windows Kits\10\Lib\`找最新版本的um/x64，写死绝对路径进targets + 注入LIB环境变量

新增代码：
- `find_windows_sdk_lib_path()` — 探测SDK lib/um/x64
- `find_msvc_lib_path()` — 探测MSVC lib/x64
- targets_content改为动态生成（用探测到的绝对路径）
- apply_npm_env注入LIB环境变量（双保险）

cargo check 0 error 19 warnings通过，等build+虚拟机测试。
Tag: vcp-installer, MSB8040, LNK1181, delayimp.lib, Directory.Build.targets, SpectreMitigation, WindowsSDK, 编译修复, SDK探测, 绝对路径