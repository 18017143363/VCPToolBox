[2026-03-29] - Rosa
[13:36] 新对话开始，主人让Rosa回忆最近的话题。
通过DeepMemo+LightMemo双管齐下回忆，确认最近话题是**Comfy Cloud逆向项目**。
## 项目当前进展
- VCPDesktop挂件方案验证通过（file://协议CORS无限制）
- HAR抓包完成，12个API端点逆向
- POST /api/prompt完整破解（认证：Bearer Firebase JWT + Cookie session双层）
- PoC v2验证成功（查积分、下载图片）
- 最后停在HttpToolkit抓包筛选阶段（500+条请求待筛选）
等待主人指示继续方向。
主人纠正了Rosa的回忆方向——不是笼统的逆向进展。
[16:07] ComfyCloudHacker目录数据完整度审计
主人让Rosa查看之前的抓包数据目录E:\VCP\Plugin\ComfyCloudHacker。
## 文件清单（17项）
- 2个HAR原始抓包（41MB+50MB）
- 4个提取脚本（extract_har/har2/core/key_apis.js）
- 提取结果：comfy_api_extract.json(419KB), comfy_key_apis.json(59KB), comfy2_core.json(33KB)
- URL概览：comfy_urls_overview.txt, comfy2_urls.txt
- core_output.txt(14.6KB)
- config.env（早期Puppeteer配置）
## 审计结论：数据100%完整
comfy2_core.json包含完整API调用链（认证→WebSocket→提交→轮询→下载）
POST /api/prompt的reqBody含完整Z-Image-Base工作流（10节点全参数）
comfy2_urls.txt确认图片下载：/api/view?filename={hash}&type=output → 302 CDN
37个global_subgraphs模板URL
方案A（ComfyCloudGen VCP插件）所有素材齐全，可立即开发。
[16:20] Comfy Cloud Explorer桌面挂件v1
主人要求先做一个桌面挂件浏览Comfy Cloud的三大库。
## API端点确认
从comfy_key_apis.json中**未找到**三个目标API的响应体（文件主要是分析/遥测类请求）。
决策：直接在挂件中实时fetch，比翻HAR高效。
## 三个API端点
| 库 | 端点 | 认证需求 |
|---|------|---------|
| 模板库 | GET /api/global_subgraphs | 可能免认证（在auth/session之前就被请求） |
| 模型库 | GET /api/assets?include_tags=models&limit=500 | 需要Bearer JWT |
| 节点库 | GET /api/object_info | 需要Bearer JWT |
## 挂件设计
- 三Tab切换：模板库/模型库/节点库
- JWT输入框+连接按钮
- 每个Tab带搜索框
- 模板库：卡片网格布局
- 模型库：卡片网格+类型标签+大小
- 节点库：按category分类树，可展开/折叠
- 数据缓存避免重复请求
## 推送状态
DESKTOP_PUSH已推送到桌面。
主人反馈模板库只有31个不完整。
## 排查结果
`/api/global_subgraphs`返回的是"子图蓝图"（31个工具类节点模板），**不是**网页上的模板商店。
网页模板商店（含SDXL、视频、LLM等分类、Popular排序）的API端点**未在两次HAR抓包中被捕获**——当时没有打开模板页面。
## 下一步
需要主人在模板商店页面补一次F12抓包，找到真正的模板列表API端点。

[17:15] 模板库API深度探测 — 全部失败记录
## 已排除的端点
| 端点 | 结果 | 结论 |
|------|------|------|
| GET /api/global_subgraphs | 返回31个 | 这是"子图蓝图"（工具类节点模板），不是模板商店 |
| GET /api/workflow_templates | 返回 {} | 可能是用户私有模板（主人没保存过所以空） |
| GET /api/workflow_templates?limit=50 | 返回 {} | 加参数也没用 |
| POST /api/workflow_templates | 401 | 不支持POST |
| GET api.comfy.org/workflow-templates | 404 | 不在这个域名 |
| GET api.comfy.org/templates | 404 | 不在这个域名 |
| __NEXT_DATA__ | undefined | 不是Next.js SSR |

## 其他发现
- POST /api/internal/cloud_analytics — 遥测上报，无关
- 模板缩略图是.webp文件，懒加载（滑动时请求）
- comfy-user请求头为空值，可能相关

## 下一步方向
模板数据可能在页面首次加载时随某个大体积请求返回。需要在Network面板（Fetch/XHR过滤 + Preserve log + 按Size降序）刷新页面捕获。
Rosa之前的Console拦截指导太混乱把主人搞晕了，需要简化指引。

[17:30] Comfy Cloud Explorer v2推送
主人放弃继续找模板商店API（确认Network面板没有更多相关请求），让Rosa把所有已知/api子路径加进面板。
## v2改动
| Tab | API | 变化 |
|-----|-----|------|
| 子图蓝图 | /api/global_subgraphs + /api/global_subgraphs/{name} | 自动分类+点击查看详情（节点列表+原始JSON） |
| 模型库 | /api/assets?include_tags=models&limit=500&include_public=true | 加include_public参数 |
| 节点库 | /api/object_info | 不变 |
| 私有模板(新) | /api/workflow_templates | 新增Tab |
## 已确认的全部API端点
cloud.comfy.org: /api/global_subgraphs, /api/global_subgraphs/{name}, /api/assets, /api/object_info, /api/workflow_templates, /api/prompt, /api/jobs, /api/view, /api/features, /api/settings, /api/extensions, /api/system_stats, /api/users, /api/auth/session, /api/internal/cloud_analytics(遥测)
api.comfy.org: /customers/cloud-subscription-status, /releases

[17:40] Comfy Cloud Explorer v3推送
主人反馈子图详情404+要求加入全部端点。
## v3改动
- 修复子图详情404：加encodeURIComponent编码name参数
- 10个Tab分两行（数据5个+系统5个）
- 新增任务列表：/api/jobs分running和completed两组
- 新增订阅积分：跨域api.comfy.org
- 新增功能开关：ON/OFF可视化
- 新增用户设置：键值对列表
- 新增扩展列表：卡片展示
- 新增系统状态：原始JSON展示
- 模型库自动翻页：最多4次×500=2000个

[17:23] PowerShell实测API返回结构
JWT已过期（exp=1774776944），4个端点报错，2个成功。
## 成功端点
### /api/extensions → 字符串数组
169个元素，每个是JS文件路径（如"/extensions/ComfyUI-LTXVideo/js/xxx.js"），**不是对象数组**。挂件需要修改渲染逻辑。
### /api/jobs → 标准分页结构
{jobs:[{id,status,create_time,execution_start_time,execution_end_time,outputs_count,preview_output,execution_error,workflow_id}], pagination:{has_more,limit,offset,total}}
## 重大发现：云端可用模型列表！
jobs的failed任务的error信息暴露了Comfy Cloud云端实际可用的模型：
**Checkpoints(10个)**: Illustrious-XL-sdxl, NetaYumev35, aMixIllustrious, flux1-dev-fp8, flux_dev, gsESCAPEFROMDISNEYDREAMS, ltx-2-19b-dev, nova3DCGXL_ilV90, realvisxlV50, waiMatureIllustrious_v20
**Upscale(4个)**: 4x-UltraSharp.pth, 8xNMKDFaces160000G_v10.pt, RealESRGAN_x4plus_anime_6B.pth, remacri_original.safetensors
**LoRA**: 28个（列表被截断，未完整显示）
这些模型列表来自failed job的error信息（PowerShell输出被截断，LoRA不完整），不在HAR抓包文件中。
## core_output.txt实际API返回结构确认
| 端点 | 返回结构 |
|------|---------|
| /api/settings | 7个键值对对象（Comfy.Assets.UseAssetAPI等） |
| /api/users(有JWT) | {migrated:true, storage:"server"} |
| /api/auth/session | {expiresIn:7200, success:true} + Set-Cookie |
| api.comfy.org/subscription | {subscription_tier:"FREE", has_fund:true, renewal_date:...} |
| /api/jobs | {jobs:[], pagination:{has_more,limit,offset,total}} |
| /api/extensions(PowerShell实测) | 字符串数组169个（JS路径） |
## 挂件修复方向
settings: 7个键值对，用键值表渲染
subscription: 跨域api.comfy.org，固定字段集
extensions: 字符串数组不是对象数组
features/system_stats: 未在抓包中出现，需新JWT测试
Tag: 日记, API实测, core_output.txt分析, 返回结构确认, 挂件修复方向, VCPDesktop，而是"怎么用VCPDesktop实现与Comfy Cloud的无障碍交流"。通过DeepMemo精确回忆，成功对上完整上下文。上次停在日记文档归档完成，下一步是开发完整的ComfyCloud桌面挂件。
Tag: 日记, 新对话, 回忆, Comfy Cloud逆向, VCPDesktop, 挂件开发, 项目进展回顾