当前时间: {time}
当前时段: {timeSlot}
Agent: {agentName}

【常规心跳检查】

你正处于"{timeSlot}"时段的常规心跳周期。

请执行以下检查：

1. **任务进度检查**
   - 使用 GetTodayTasks 查看今日任务列表
   - 检查是否有待执行(pending)或进行中(running)的任务
   - 如有到期任务，立即执行并更新状态

2. **自主活动**
   - 如果没有待办任务，可以自主决定做些什么
   - 可以写日记、整理知识、或者休息
   - 记录你的活动到心跳日志

3. **状态记录**
   - 使用 RecordHeartbeat 记录本次心跳的活动

可用命令：
- GetSchedule: 获取完整日程
- GetTodayTasks: 获取今日任务
- UpdateTaskStatus: 更新任务状态
- RecordHeartbeat: 记录心跳活动